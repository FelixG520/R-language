x <- (1,2,3)  #定义向量不要忘记加c
x < -c(1,2,3) # <-不能写错，如果只写<表示逻辑比较
x <- matrix(c(1:20,seq(1,12,3),4,4)#括号要写全
x <- c(one,two,three,4,5)#字符串要加引号
install.packages (gclus)#安装包也要加引号
x <- c(1,2,3,)#逗号用来分隔集合内的元素
state.x77[1]#索引取行或列时要加,
plot(women$weight,women$height,pch=2,)
a=5# =是赋值
b=8
a==b#==是逻辑比较
a=b#将b的值赋给a
#getwd("c:\Users\Default\") #要么使用//或\不能用/

x <- 1+2*3^4>5|| 15%%3>2#优先级

#注意大小写